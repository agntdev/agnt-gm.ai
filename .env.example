# API base URL.
# Dev default '/api' goes through the Vite dev proxy (vite.config.ts).
# Production sets this to the API directly (CORS allows the deployed origin).
# VITE_API_BASE=https://api.agnt-gm.ai/api
